/*:
 # Dictionaries — Basics
 Goal: Create and access key–value pairs with optional results.
 */

import Foundation

// 1) Make a dictionary of airport codes to city names.
//    Example: "JFK": "New York"
// TODO: var airports: [String: String] = ["JFK": "New York", ...]

// 2) Read a value with a known key and print it (non-optional via ! only if sure).
// TODO: print(airports["JFK"]!)

// 3) Safely read a value for a key that may not exist (use if let).
// TODO: if let city = airports["SFO"] { print(...) } else { print("Not found") }

// 4) Add a new entry and update an existing one.
// TODO: airports["SFO"] = "San Francisco"
// TODO: airports["JFK"] = "NYC"

// 5) Remove an entry by setting to nil. Verify removal.
// TODO: airports["SFO"] = nil
// TODO: print(airports)
