/*:
 # Arrays — Mutation, Search, Sort
 Goal: Practice common array operations and searching.
 */

import Foundation

var scores = [92, 85, 100, 76, 85]

// 1) Add two new scores, then remove the last one.
// TODO: scores.append(...); scores.append(...)
// TODO: scores.removeLast()

// 2) Find the first index of 85 (if present) and print it.
// TODO: if let i = scores.firstIndex(of: 85) { ... }

// 3) Sort ascending, then descending.
// TODO: scores.sort()
// TODO: scores.sort(by: >)

// 4) Make a NEW array of passing scores (>= 80) using filter.
//    Print both arrays to show you didn’t mutate the original by mistake.
// TODO: let passing = scores.filter { ... }
// TODO: print("All:", scores); print("Passing:", passing)

// 5) Map scores to letter grades (A >= 90, B >= 80, else C).
// TODO: let letters = scores.map { ... }
// TODO: print("Letters:", letters)
